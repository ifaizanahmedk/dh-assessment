import { COLOR } from "./color";
import { FONT } from "./font";

const ThemeConfig = {
	colors: {
		primary: COLOR.GREEN_NEON,
		background: COLOR.WHITE_SNOW,
		boxShadow: "0 4px 8px rgba(0, 0, 0, 0.1)",
	},
	fonts: {
		primary: FONT.PRIMARY,
		secondary: FONT.SECONDARY,
	},
};

export default ThemeConfig;
